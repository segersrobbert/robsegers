// dimensions and margins
export const width = window.innerWidth;
export const height = 0.65 * window.innerHeight;
export const svgWidth = 0.85 * width;
export const margin = {
  top: (0.05 * height),
  right: (0.05 * width),
  bottom: (0.05 * height),
  left: Math.max((0.05 * width), 30)
};
export const axisRightColor = 'steelblue';
export const axisLeftColor = 'darkred';
